server:
  port: 6969

spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
        - id: USER-SERVICE
          uri: lb://USER-SERVICE
          predicates:
            - Path=/users/**
        - id: MSG-SERVICE
          uri: lb://MSG-SERVICE
          predicates:
            - Path=/messages/**